import {Otterbots} from "../otterbots";

// Get bot instance
const bot = new Otterbots();

// Start the bot
bot.start();
bot.setActivity("playing", "Genshin Impact")

// Start tasks (if you not use tasks, you can delete this)
bot.initTask()